# Full dependencies for AI Emotion Detection
# Includes all deep learning frameworks
# Developed by Aravind

# Core computer vision
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0

# Face detection
mediapipe>=0.10.0

# Numerical computing
numpy>=1.24.0

# CRITICAL: Protobuf version must be compatible with both MediaPipe and TensorFlow
protobuf>=3.20.3,<4.0

# ONNX runtime for efficient DL inference
onnxruntime>=1.16.0

# Deep learning frameworks
torch>=2.0.0
transformers>=4.30.0

# TensorFlow (install before DeepFace to control version)
tensorflow>=2.13.0,<2.16.0

# DeepFace for advanced emotion detection
deepface>=0.0.79

# Utilities
requests>=2.31.0

# Note: If you still encounter protobuf errors, try:
#   pip uninstall protobuf -y
#   pip install protobuf==3.20.3
